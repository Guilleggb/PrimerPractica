#Ejercicio 1: 

# 1ro Determinamos cual es el directorio utlizando pwd
# 2do Utilizamos ls para ver los archivos y ls -a para ver tambien los archivos ocultos
# 3ro Utilizamos el comando stat para mostrar información detallada sobre un archivo o directorio
# 4to Utilizamos ls -sh / ls -s para mostrar el tamaño de los archivos o directorios
# 5to Guardamos la salida del anterior comando en main.sh, igual hicimos la carpeta del ejercicio cuatro
# 6to El comando ls -l se utiliza en la línea de comandos para listar los archivos y directorios de un directorio específico en formato detallado
# 7mo Con mkdir creamos una carpeta y con cd fuimos a la carpeta, para despues imprimir en un archivo el nuevo directorio
# 8vo El comando ps -e se utiliza para mostrar todos los procesos en ejecución en el sistema, incluidos los procesos de todos los usuarios
# 9no Cree un archivo que contiene un listado de los archivos de la carpeta home
#10 extra: Utilizamos history y despues el nro de el comando que queremos eliminar así: history y en el siguiente reglon !n (nro del comando a eliminar) El comando se elimina si es reversible

#VER El comando cat se utiliza para mostrar el contenido de archivos de texto en la salida estándar o combinar varios archivos en uno solo